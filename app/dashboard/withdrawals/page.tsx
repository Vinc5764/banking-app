import PendingTransaction from "@/components/PendingTransactions";
import React from "react";

const page = () => {
  return (
    <div>
      <PendingTransaction />
    </div>
  );
};

export default page;
